import { createAction } from '@ngrx/store'

export const getFolders = createAction('[Folders] Get')
export const postFolders = createAction('[Folders] Post')
export const putFolders = createAction('[Folders] Put')
export const deleteFolders = createAction('[Folders] Delete')
